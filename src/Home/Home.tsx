import React from 'react';
// components
import {Text, View} from 'react-native';

const Home = (): JSX.Element => {
    return (
        <View>
            <Text>JAJAJA</Text>
        </View>
    );
};

export default Home;
